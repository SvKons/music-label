export interface IArtists {
    id: string;
    url?: string;
    author?: string | number;
    title?: string;
    imgUrl?: string;
}

export interface IArtistsCard {
    id: string;
    url?: string;
    author?: string | number;
    title?: string;
    imgUrl?: string;
}
